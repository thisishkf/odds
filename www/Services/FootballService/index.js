const odds500 = require(__dirname + '/odds500');
const hkjc = require(__dirname + '/hkjc');
const analyse = require(__dirname + '/analyse');

module.exports = {
    ODDS500Service: odds500,
    HKJCService: hkjc,
    analyseService: analyse
}